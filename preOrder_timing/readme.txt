For running the preordering scripts n times and computing the average runtime. 
The scripts run on the randomly shuffled data in case of real networks.
For saving the IO time, no file is written to the output (except for the output of Gorder after the first run: that is needed for the next code)
